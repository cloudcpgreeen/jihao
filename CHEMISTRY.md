# CHEMISTRY

**第五融合：多对象反应网络中的 FUSE**

不预设自组织。不引入生命。不发明守恒律。

只做一件事：**当对象可以互相转换时，FUSE 的三问发生什么？**

---

## 0. 与 MATTER 的关键区别

```
MATTER:     一个对象 + 力场 → 对象属性变化，对象本身不变
CHEMISTRY:  多个对象 → 对象可以拆分、重组、数量改变
```

```
MATTER:     M → M'                          (n = 1, 永远是同一个对象)
CHEMISTRY:  (A, B, ..., N) → (C, D, ..., M)  (n ≠ m 是合法的)
```

**这是第一次：变换改变了对象的数量和种类。**

---

## 1. 变：反应作为变换

```
R: (A, B) → (C, D)

具体例子：
  H₂ + O → H₂O          (2 个对象 → 1 个对象)
  H₂O → H₂ + O          (1 → 2)
  HCl + NaOH → NaCl + H₂O  (2 → 2)
```

### 形式化

```
X = (n_A 个 A, n_B 个 B, ..., n_N 个 N)    状态向量（各物质的量）

R: X → X' = X + ν·ξ

其中 ν = (ν_A, ν_B, ..., ν_N) 是化学计量系数向量
      ξ 是反应进度（一次反应消耗/生成多少）
```

| 条件 | 成立？ |
|------|--------|
| 变换存在 | ✅ 反应定义了状态→状态的映射 |
| n ≠ m | ✅ 对象数量可变 |
| 可逆 | ⚠️ 取决于反应是否可逆（有些是，有些不是） |

**变 ✓**

---

## 2. 合：反应网络

### 2.1 两步反应

```
R₁: A + B → C
R₂: C + D → E

R₂ ∘ R₁: (A + B + D) → E
         （先消耗 A,B 生成 C，C 再与 D 反应生成 E）
```

| 条件 | 成立？ |
|------|--------|
| 组合存在 | ✅ R₂ ∘ R₁ 是合法的反应序列 |
| 封闭性 | ⚠️ 组合结果依赖于中间物 C 是否被消耗完 |
| 结合律 | ✅ (R₃ ∘ R₂) ∘ R₁ = R₃ ∘ (R₂ ∘ R₁)（反应顺序与分组无关） |

**合 ✓**

### 2.2 反应网络

多步反应形成有向图：

```
       A + B
         │
       R₁│
         ▼
         C
       ↙   ↘
    R₂│     │R₃
      ▼     ▼
      D     E
       \   /
      R₄\ /R₅
         ▼
         F
```

**这回到了最初的 Composition —— 但不再只是程序能力连接，而是对象之间真实的物质转换。**

### 2.3 反应网络的代数结构

```
若 R₁, R₂ 都是合法反应
且 R₁ 的产物包含 R₂ 的反应物
则 R₂ ∘ R₁ 是合法反应序列

反应网络 = (状态空间, {R₁, R₂, ...}, ∘)
         = 一个由反应生成的变换半群
```

---

## 3. 守：寻找守恒向量

### 3.1 候选清单

对反应 H₂ + O → H₂O：

| 候选不变量 | 反应前 | 反应后 | 保持？ |
|-----------|--------|--------|--------|
| 总质量 | m_H₂ + m_O | m_H₂O | ✅ |
| H 原子数 | 2 | 2 | ✅ |
| O 原子数 | 1 | 1 | ✅ |
| 总电荷 | 0 + 0 | 0 | ✅ |
| 总能量 | E_H₂ + E_O | E_H₂O + ΔE | ⚠️ 看反应条件 |
| 分子数 | 2 | 1 | ❌ |
| 总键能 | 取决于具体键 | 不同 | ❌ |

**三个最稳定的不变量：元素计数、总电荷、总质量。**

### 3.2 从标量到向量

单个反应可能同时保持多个量：

```
I = (I_H, I_O, I_charge, I_mass)

对 H₂ + O → H₂O:
  I_H    = H 原子数: 2 → 2  ✓
  I_O    = O 原子数: 1 → 1  ✓
  I_charge = 总电荷:  0 → 0  ✓
  I_mass = 总质量:   不变   ✓
```

**这是 invariant vector —— 不是找一个不变量，是找一组不变量。**

### 3.3 守恒子空间

```
状态向量 x = (n_A, n_B, ..., n_N)
守恒矩阵 C（每行是一个守恒约束）
守恒律: C·x = C·x' = constant

对 H₂ + O ↔ H₂O 系统：
  x = (n_H₂, n_O, n_H₂O)

  守恒约束:
    2·n_H₂ + 0·n_O + 2·n_H₂O = H_atoms_const    (H 原子守恒)
    0·n_H₂ + 1·n_O + 1·n_H₂O = O_atoms_const    (O 原子守恒)

  C = [2 0 2]
      [0 1 1]

  C·x = constant（对所有合法反应序列）
```

**C·x = constant 定义了状态空间中的一个仿射子空间。** 反应只能在守恒子空间内移动。

### 3.4 Orbit + Polaris 重逢

```
             守恒子空间 (Polaris)
           ╱─────────────────╲
          /    • → • → •       \
         /    /         \       \
        • → •             • → •  \
       /                            \
      /      Chemistry Orbit          \
     ╱──────────────────────────────────╲
```

**轨迹在动。边界不动。**

```
Polaris:    C·x = constant（守恒子空间 = 不变量）
Orbit:      反应网络在守恒子空间内产生的轨迹
```

**这是第一次：变 (reaction) + 合 (network) + 守 (conserved subspace) 同时在一个系统中出现。**

### 3.5 不变量类型对比

| 融合 | 不变量类型 | 数学性质 |
|------|-----------|---------|
| TIME | \|archive\| | 单调非递减 |
| SCALE | TIME 不变量保持 | 不变量的不变性 |
| SPACE | — | OPEN |
| MATTER | v, p, E | 守恒（依赖力场） |
| CHEMISTRY | C·x = const | **守恒向量（多重约束）** |

---

## 4. FUSE(CHEMISTRY) 结果

```
变 ✓   A + B → C + D
       对象数量和种类都可变
       反应定义了状态空间的变换

合 ✓   R₂ ∘ R₁ 合法
       反应网络形成有向图
       组合 = 物质转换的半群

守 ✓   invariant vector C·x = const
       元素计数、电荷、质量（封闭系统）
       守恒子空间约束所有反应轨迹
```

**三问全过。冻结。**

---

## 5. 当前无法统一的部分

| 缺口 | 严重性 | 说明 |
|------|--------|------|
| 反应可逆性 | 中 | 不可逆反应没有 R^{-1}，半群不是群 |
| 能量守恒 | ⚠️ 条件依赖 | 只在孤立系统中严格成立 |
| 反应动力学速率 | 低 | FUSE 只关心结构，不关心速率 |
| C 矩阵是否完整 | 中 | 当前只列了元素和电荷，是否有其他？ |

---

## 6. 给 LIFE 的出口

CHEMISTRY 交付的是一张守恒子空间图：

```
反应可以改变对象种类和数量。
但守恒向量 C·x = const 把所有反应限制在一个子空间内。
```

**LIFE 要问的是：**

```
如果一个反应网络不仅被动地被守恒律约束，
还能主动维持某些结构（而这些结构不是守恒量本身），
那 FUSE 的三问会发生什么？

变:  仍然成立
合:  仍然成立
守:  是否出现了新的不变量类型？
     — 不是元素守恒（那是化学的）
     — 不是能量守恒（那是物理的）
     — 而是……？
```

**这是真正的问题。现在不回答。留给 LIFE。**

---

## 7. 融合链状态

```
STRUCTURE → TIME → SCALE → SPACE → MATTER → CHEMISTRY → LIFE
    ✓        ✓       ✓      ✓(OPEN)   ✓          ✓          ?
                                                  │
                                         变 ✓  合 ✓  守 ✓
                                         invariant vector
                                         C·x = const
                                         Orbit + Polaris 重逢
```

---

## 冻结

CHEMISTRY.md 不预设生命。不引入自组织。不发明守恒律。

只验证了一件事：**多对象反应网络可以完美容纳在 FUSE 框架内。** 守从单一守恒量升级为守恒向量，守恒子空间同时是 Polaris（不动边界）和 Orbit（反应轨迹）的承载面。

```
FUSE(CHEMISTRY):  变 ✓  合 ✓  守 ✓  (invariant vector)
```

下一融合：LIFE —— 主动维持结构是否产生新类型的不变量？
