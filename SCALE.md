# SCALE

**第二融合：尺度变换的组合律，以及时间—尺度一致性假设**

不增加新概念。不引入物理解释。不写代码。

只研究一个问题：**同一个结构，换一个观察尺度以后，还是不是同一个结构？**

---

## 1. 核心公理

```
R_1 = I                                    (1)
R_λ₁ ∘ R_λ₂ = R_{λ₁λ₂}                     (2)
```

(1) 单位尺度变换 = 恒等变换。不放大也不缩小。
(2) 两次尺度变换 = 尺度相乘后的一次变换。

**对比 TIME**：

```
TIME                          SCALE
─────────────────────         ─────────────────────
T_0 = I                       R_1 = I
T_τ₁ ∘ T_τ₂ = T_{τ₁+τ₂}      R_λ₁ ∘ R_λ₂ = R_{λ₁λ₂}
       ↑                                ↑
   加法半群                         乘法半群
   (ℕ, +)                          (ℝ_{>0}, ×)
```

加法 vs 乘法。这不是巧合 —— 时间累积是加性的，尺度放大是乘性的。两个世界都是半群，但运算不同。

---

## 2. λ 的参数域：从 ℕ 到 ℝ

```
计数 → ℕ → ℤ → ℚ → ℝ
```

### 2.1 离散尺度：λ ∈ ℕ

```
λ = 1: 原始尺度（不缩放）
λ = 2: 两倍
λ = 3: 三倍
...

R_2 ∘ R_3 = R_6  ✅  放大 3 倍再放大 2 倍 = 放大 6 倍
R_1 = I          ✅  单位尺度，不变
```

**在代码中**：整数倍是最自然的尺度变换。

| 现有概念 | 尺度变换 R_λ |
|----------|-------------|
| Outbox 轮询间隔 | `Duration::from_secs(1)` → `Duration::from_secs(λ)` |
| Mailbox attention_max | `3` → `λ·3` |
| User 实例数量 | N → λ·N |

### 2.2 连续尺度：λ ∈ ℝ_{>0}

```
λ = 0.5:  缩小一半（更高分辨率）
λ = √2:   无理数倍缩放
λ → 0⁺:   无限放大（微观极限）
λ → ∞:    无限缩小（宏观极限）
```

**当前代码没有连续尺度。** 所有参数都是离散的（整数秒、整数容量）。但连续尺度是物理学的入口 —— 如果结构只能在 ℕ 上成立而在 ℝ 上失败，那就知道融合的边界在哪里。

### 2.3 ℕ → ℝ 的数学展开

```
ℕ:   一个、两个、三个...        （计数）
ℤ:   ...、−2、−1、0、1、2...   （可逆）
ℚ:   p/q，比例                  （有理缩放）
ℝ:   连续统                      （光滑缩放）

当前代码：ℕ
TIME 已证明：ℕ 上离散半群成立
SCALE 要问：  能否扩展到 ℝ_{>0}？
```

**关键**：R_λ 在 ℕ 上是良定义的。但 R_0.5（一半尺度）不存在于当前代码。要进入连续尺度，需要定义"半个轮询周期"或"连续极限"。

---

## 3. 现有代码中的尺度变换

### 3.1 Outbox 轮询间隔

**代码**：`src/outbox/mod.rs:25` — `Duration::from_secs(1)`

**形式化**：
```
Δ = 基础轮询间隔 (1s)
R_λ: Δ → λ·Δ

R_2: 轮询间隔变为 2s（更粗的观测）
R_0.5: 轮询间隔变为 0.5s（更细的观测，但代码目前不支持）
```

**验证**：

| 条件 | 成立？ |
|------|--------|
| R_1 = I | ✅ λ=1 时间隔不变 |
| R_λ₁ ∘ R_λ₂ = R_{λ₁λ₂} | ✅ 先缩放 λ₂ 再缩放 λ₁ = 直接缩放 λ₁λ₂ |
| 连续 λ | ❌ Duration::from_secs 接受 u64，不接受 f64 |
| 逆变换 R_{1/λ} | ❌ 代码不支持 |

**分类**：✅ 离散乘法半群 (ℕ_{>0}, ×)。与 TIME 的加法半群形成对偶。

### 3.2 Mailbox 容量

**代码**：`src/mailbox/mod.rs:23` — `attention_max: usize`

**形式化**：
```
C = 注意力缓冲区容量
R_λ: C → λ·C

R_2: 容量翻倍（更宽容，更少触发 Call）
R_0.5: 容量减半（更严格，更多触发 Call）
```

**验证**：

| 条件 | 成立？ |
|------|--------|
| R_1 = I | ✅ |
| R_λ₁ ∘ R_λ₂ = R_{λ₁λ₂} | ✅ 缩放两次 = 一次缩放乘积 |
| 不变量 | ⚠️ 容量变化改变了 Call 触发频率 —— 行为改变，但 Archive 单调性保持 |

**分类**：✅ 离散乘法半群。但改变容量的效果值得注意 —— 这不是纯观测变换，它改变系统行为。

### 3.3 State Algebra 维度

**代码**：`cloudos-runtime-core/src/state_algebra/mod.rs`

**形式化**：
```
状态空间 = { [d₀, d₁, d₂] | dᵢ ∈ {0, 1} }
总状态数 = 2³ = 8

问：维度数 3 本身可以被缩放吗？
答：不能。3 是固定的 —— Identity, Relation, Action 是冻结语义。
```

**分类**：ℹ️ 当前不可缩放。State Algebra 的维度数是一个**固定结构常数**，不是自由参数。这不意味着融合失败 —— 有些结构在尺度变换下保持常数本身就是一种不变量。

---

## 4. 尺度不变量

### 4.1 现有不变量的尺度稳定性

**来自 TIME.md 的两个不变量**：

```
A3. |archive| 在 T_τ 下非递减
```

**问**：这个不变量在 R_λ 下保持吗？

```
答：是。Archive 单调性不依赖于轮询间隔或缓冲区容量。
    无论 λ 多大，消息一旦进入 archive，永远不会被删除。
    I_archive(R_λ S) ≥ I_archive(S) 对所有 λ 成立。
```

```
A4. I_identity(S) = true ⇒ I_identity(T_τ S) = true
```

**问**：这个不变量在 R_λ 下保持吗？

```
答：是。Identity 的 Aligned 状态不依赖于观测尺度。
    你放大或缩小观测，不影响"你是谁"。
    I_identity(R_λ S) = I_identity(S) 对所有 λ 成立。
```

**分类**：✅ 两个 TIME 不变量都是**尺度不变的**。这是第一个跨融合的正面结果。

### 4.2 尺度特有的不变量

```
S1. 组合律结构不变：
    R_λ ∘ R_μ 的结构（乘法半群）在所有尺度下保持一致。
    这个结构是 λ 取值无关的。

S2. 恒等变换不变：
    R_1 = I 对所有系统参数成立。单位尺度永远不改变任何东西。

S3. 正性：
    λ > 0 始终成立。不存在"负尺度"（那将是反射，不是缩放）。
    尺度变换永远保持方向。
```

### 4.3 尺度敏感的量（非不变量）

这些量在 R_λ 下改变 —— 它们是尺度相关的：

| 量 | R_λ 的效果 | 类别 |
|----|-----------|------|
| 轮询延迟 | 线性缩放：延迟 → λ·延迟 | 尺度协变 |
| Call 触发频率 | 非线性缩放：取决于消息到达率 | 尺度响应 |
| Outbox 吞吐量 | 反比缩放：吞吐量 → 吞吐量/λ | 尺度协变 |

**区分协变和不变量是物理学的起点。**

---

## 5. 时间—尺度一致性假设

这是 SCALE 把门打开的核心问题。

### 5.1 问题的提出

```
已知：
  T_τ: 时间平移，满足 T_τ₁T_τ₂ = T_{τ₁+τ₂}
  R_λ: 尺度变换，满足 R_λ₁R_λ₂ = R_{λ₁λ₂}

问：
  T 和 R 之间满足什么关系？

假设（待验证）：
  R_λ T_τ = T_{f(λ,τ)} R_λ

  其中 f(λ, τ) 是某个函数，描述"先缩放再演化"
  需要多少时间参数才能等价于"先演化再缩放"。
```

### 5.2 最简单的候选：f(λ, τ) = λτ

```
假设 H：
  R_λ T_τ = T_{λτ} R_λ
```

**物理意义**：先改变观测尺度，再演化 τ 时间，等于先演化 λτ 时间（在原始尺度下），再改变观测尺度。

**用 Outbox 轮询解释**：
```
R_λ: 轮询间隔从 1s 变为 λ 秒
T_n: 轮询 n 次

R_λ T_n:  间隔变为 λ 秒，轮询 n 次  → 覆盖 nλ 秒的真实时间
T_{λn} R_λ: 轮询 λn 次（1s 间隔），间隔变为 λ 秒 → 覆盖 λn 秒的真实时间

R_λ T_n = T_{λn} R_λ  ✅ 对于 Outbox 轮询，f(λ,n) = λn 成立！
```

**验证**：

| 条件 | 成立？ | 理由 |
|------|--------|------|
| f(λ, τ) = λτ | ✅ 对于 Outbox 轮询 | 轮询次数 × 间隔 = 覆盖时间 |
| 组合律一致性 | ✅ | R_λ T_n T_m = T_{λ(n+m)} R_λ = T_{λn} T_{λm} R_λ |
| 恒等一致性 | ✅ | R_1 T_τ = T_τ R_1 |
| 连续 λ | ❓ 未验证 | 需要 R_{0.5}，代码不支持 |

**分类**：⚠️ **假设 H 在离散 Outbox 上成立。在连续情况下是开放问题。**

### 5.3 如果 H 成立

```
             Scale
               │
               ▼
State ──────→ State'
  │             │
 Tτ│             │T_{λτ}
  │             │
  ▼             ▼
State ──────→ State'
          Scale
```

**这是一个交换图（commuting diagram）。** 它断言：尺度变换和时间演化是相容的 —— 你先走哪条路，最终到达同一个等价状态。

### 5.4 如果 H 不成立

如果存在某个 λ 和 τ 使得：
```
R_λ T_τ ≠ T_{f(λ,τ)} R_λ  对任何合理的 f
```

那就意味着**时间和尺度之间存在不可消除的张力**。

这正是科学：不是宣布统一，而是精确知道**哪里不统一**。

---

## 6. 对偶结构：加法半群 vs 乘法半群

这可能是 SCALE 最漂亮的结构发现。

```
TIME 参数空间        SCALE 参数空间
─────────────────    ─────────────────
(ℕ, +)               (ℝ_{>0}, ×)
半群运算: 加法       半群运算: 乘法
单位元: 0           单位元: 1
逆元: 不存在         逆元: 1/λ（连续时存在）
                     
τ₁ + τ₂ = τ₂ + τ₁   λ₁ × λ₂ = λ₂ × λ₁
（交换）              （交换）

T_0 = I              R_1 = I
```

**关键洞察**：时间平移和尺度变换是**同一个抽象模式（参数化半群作用在状态空间上）**的两个具体实例。它们的区别仅在于参数空间的代数结构：
- 时间用加法组织参数
- 尺度用乘法组织参数

这意味着存在一个更底层的结构：

```
TransformationAlgebra {
    parameter_space: Semigroup,
    action: State → State,
    identity: 参数空间.单位元 → I,
    composition: 参数空间.运算 → ∘,
}
```

TIME 和 SCALE 都是这个抽象结构的实例。

**这正是 STRUCTURE.md 所说的：不是把所有东西说成一件事，而是找到同一个结构究竟能走多远。**

---

## 7. 三个层级

| 层级 | 数学性质 | CODE 证据 |
|------|----------|----------|
| **1. 乘法半群** ✅ | R_λ₁R_λ₂ = R_{λ₁λ₂}，R_1 = I | Outbox 间隔、Mailbox 容量 |
| **2. 时间-尺度一致性** ⚠️ | R_λ T_τ = T_{λτ} R_λ | Outbox 轮询上成立，通用性未验证 |
| **3. 连续尺度** ❓ | λ ∈ ℝ_{>0}，光滑缩放 | 代码不支持，纯形式化 |

---

## 8. 不变量汇总

| 不变量 | 来源 | 在 T_τ 下 | 在 R_λ 下 |
|--------|------|----------|----------|
| Archive 单调性 | TIME A3 | ✅ 非递减 | ✅ 不变 |
| Identity 连续性 | TIME A4 | ✅ 保持 | ✅ 不变 |
| 半群结构 | SCALE S1 | — | ✅ 不变 |
| 恒等变换 | SCALE S2 | ✅ | ✅ |
| λ 的正性 | SCALE S3 | — | ✅ |

**前两个不变量跨越了 TIME 和 SCALE 的边界。** 它们不依赖参数域是 ℕ 还是 ℝ，不依赖运算是加法还是乘法。

**这是第一次融合真正的证据：不变量比它们所属的变换框架更深。**

---

## 9. 当前无法统一的部分

| 缺口 | 严重性 | 需要的条件 |
|------|--------|-----------|
| 只有离散尺度 ℕ，无连续尺度 ℝ | 高 | 引入连续极限 |
| 时间-尺度一致性仅在 Outbox 上验证 | 中 | 在其他子系统上测试 |
| f(λ,τ) 是否唯一确定 | 中 | 推导 f 的一般形式 |
| R_λ 对 State Algebra 维度的缩放不存在 | 低 | 维度假定固定为 3 |

---

## 10. 判断：第二融合的结论

```
问：尺度变换是否形成半群？
答：是。R_λ₁R_λ₂ = R_{λ₁λ₂}，乘法半群。与 TIME 的加法半群形成对偶。

问：现有不变量在尺度变换下是否保持？
答：是。Archive 单调性和 Identity 连续性都是尺度不变的。

问：时间-尺度一致性 R_λ T_τ = T_{λτ} R_λ 成立吗？
答：在 Outbox 轮询上成立。在其他子系统上未验证。这是核心开放问题。

问：ℕ → ℝ 的过渡在哪里受阻？
答：所有现有代码都使用整数参数。连续尺度不存在于实现中。
    数学上可以定义，但尚未触及。
```

---

## 11. 给下一融合的出口

```
SCALE
  │
  ├── 乘法半群          → 已证明（ℝ_{>0}, ×）
  ├── 时间-尺度一致性   → 假设 H（部分验证）
  ├── 尺度不变量        → 跨 TIME 边界成立
  │
  └── 缺口 → SPACE：时间-尺度相容性 → 时空统一，c : Time ↔ Space
```

第二融合的**核心交付**不是答案，而是**时间-尺度对偶表**：

```
         TIME          SCALE
         ────          ─────
运算     +             ×
单位元   0             1
参数域   ℕ             ℝ_{>0}（理想）
半群     T_τ₁T_τ₂      R_λ₁R_λ₂
         = T_{τ₁+τ₂}   = R_{λ₁λ₂}
```

**这不是两个不同的结构。这是同一个抽象模式在两种运算下的两次出现。**

如果下一融合（SPACE）能找到 c : Time → Space 的自然映射，那么对偶表将扩展为：

```
TIME ←──c──→ SPACE
  │            │
  +            ?（空间组合律是什么？）
  │            │
  ℕ            ?（空间参数域是什么？）
```

---

## 冻结

SCALE.md 不引入物理解释。不新增哲学。不等价于"宣布光年是尺度"。

它钉住了一个问题和一个对偶：

**问题**：R_λ T_τ = T_{λτ} R_λ 是否普遍成立？

**对偶**：加法半群 (ℕ, +) 和乘法半群 (ℝ_{>0}, ×) 是同一个变换代数模式的两个实例。

此文件冻结。下一融合：SPACE.md（当用户要求时）。

```
073ed52 → TIME → SCALE → SPACE → MATTER → CHEMISTRY → LIFE
  ✓        ✓      ✓        ?         ?          ?        ?
```
