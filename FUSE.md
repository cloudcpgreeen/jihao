# FUSE

**融合算法：变 → 合 → 守**

不证明宇宙。不发明答案。OPEN 是合法结果。

---

## 算法

```
FUSE(X):
  1. 变 — 存在变换 T: X → X' 吗？
     NO  → 停。不可融合。
     YES → 继续。

  2. 合 — T₁ ∘ T₂ 满足组合律吗？
     NO  → 记录，冻结。
     YES → 记录组合律，继续。

  3. 守 — 存在不变量 I: I(TX) = I(X) 吗？
     NO  → 标 OPEN。冻结。
     YES → 证明 I，冻结。
```

**绝不发明 I。OPEN 是保护诚实。**

---

## 已完成的融合

| 融合 | 变 | 合 | 守 | 状态 |
|------|----|----|-----|------|
| TIME | ✅ T_τ: S_t → S_{t+τ} | ✅ T_τ₁T_τ₂ = T_{τ₁+τ₂} | ✅ \|archive\| 非递减 | **冻结** |
| SCALE | ✅ R_λ: S → R_λ S | ✅ R_λ₁R_λ₂ = R_{λ₁λ₂} | ✅ I_TIME 尺度不变 | **冻结** |
| SPACE | | | | **进行中** |

---

## 记忆口诀

```
变 → 合 → 守

Transform → Compose → Preserve

变形金刚：

  变了什么？
  怎么合？
  什么没变？
```

三问能答全 → 冻结。
只能答两问 → 留 OPEN。
一问都答不了 → 不融合。

---

## 禁止事项

- 不为了闭合而发明不变量
- 不把假设当证明
- 不把诗歌当数学
- 不把物理当结构（除非物理自己从结构中长出来）
- OPEN ≠ 失败。OPEN = 知道边界在哪里。

---

冻结。此文件是融合的规则，不是融合的内容。
