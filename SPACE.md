# SPACE

**第三融合：时间→空间的结构保持映射**

```
FUSE(SPACE):
  1. 变 — C_c: t → x = ct
  2. 合 — C_c(τ₁+τ₂) = C_c(τ₁) + C_c(τ₂)
  3. 守 — ?
```

---

## 1. 变：时间映射到空间

```
C_c: Time → Space
C_c(τ) = c·τ

其中 c 是一个固定转换常数。
```

**物理直觉**：1 光年 = c × 1 年。光年不是新对象 —— 是时间经过固定转换得到的空间尺度。

**代码对应**：当前代码中没有显式的 Time→Space 映射。Address 和 Coordinate 是空间标记，但它们是静态的，不随时间演化。

**在已有结构中的位置**：

```
TIME                 SPACE
  │                    │
  │ T_τ                │ C_c
  ▼                    ▼
S_t → S_{t+τ}      t → x = ct
```

| 条件 | 成立？ |
|------|--------|
| 映射存在 | ✅ C_c 是良定义的函数 |
| 映射可逆 | ✅ C_c^{-1}(x) = x/c（c > 0） |
| 代码实现 | ❌ 无 |

**分类**：✅ 形式化存在。

---

## 2. 合：组合律被保留

TIME 有 T_τ₁T_τ₂ = T_{τ₁+τ₂}。问：C_c 是否保留这个组合结构？

```
C_c(τ₁ + τ₂) = c(τ₁ + τ₂) = cτ₁ + cτ₂ = C_c(τ₁) + C_c(τ₂)
```

**成立。** C_c 是线性映射，将 TIME 的加法半群结构完整保留到 SPACE 的加法半群。

```
  τ₁, τ₂ ──+──→ τ₁+τ₂
    │              │
  C_c            C_c
    │              │
    ▼              ▼
  x₁, x₂ ──+──→ x₁+x₂
```

| 条件 | 成立？ |
|------|--------|
| C_c 保持加法 | ✅ c(τ₁+τ₂) = cτ₁ + cτ₂ |
| 结构保持 | ✅ TIME 的 (ℕ, +) 半群映射为 SPACE 的 (cℕ, +) 半群 |
| 单位元保持 | ✅ C_c(0) = 0 |

**分类**：✅ 已证明。C_c 是半群同态 (ℕ, +) → (cℕ, +)。

---

## 3. 守：寻找不变量

这是 SPACE 三问中最难的一问。

### 3.1 C_c 自身的不变量

```
C_c 的逆：C_c^{-1}(x) = x/c
C_c ∘ C_c^{-1} = I（空间上）
C_c^{-1} ∘ C_c = I（时间上）

不变量：比率 c = x/t（对所有 t > 0 成立）
```

c 是 Polaris —— 它不随 t 变化。但这很弱：c 是映射的**参数**，不是状态在变换下的保持性质。

### 3.2 从 TIME 继承的不变量

TIME 的不变量经 C_c 映射到空间：

| TIME 不变量 | 经 C_c 映射 | 是否保持 |
|------------|------------|---------|
| \|archive\| 非递减 | 空间坐标不影响 archive | ✅ 无关（空间映射不改变 Mailbox） |
| Identity 连续性 | 空间坐标不影响 Identity | ✅ 无关（你是谁不取决于你在哪） |

TIME 的两个不变量 **与空间无关**。它们不依赖坐标，因此 C_c 自然保持它们。

但这不能算 SPACE 的不变量 —— 它们是 TIME 的不变量，只是没被 SPACE 破坏。

### 3.3 空间特有的不变量候选

当前代码中：

```
Region 层级结构：  province ⊃ city ⊃ district ⊃ detail
                 （编译时加载，运行时不改变）

UserAddress.created_at：创建后不改变

Address 的归属：    address.user_id 不变（地址属于谁）
```

但这些是数据完整性约束，不是数学不变量。

**空间中最自然的候选不变量是距离：**

```
若 x₁ = ct₁, x₂ = ct₂
则 Δx = c·Δt
即 Δx/Δt = c（常数）
```

但这和 3.1 是同一个东西 —— c 作为比率不变。

### 3.4 如果要更深：需要更多结构

真正的不变量（如 Minkowski 间隔 s² = c²t² - x²）需要：
- 不止一维空间（需要 y, z）
- 相对运动（需要参考系变换）
- 时空作为一个统一流形

**当前状态没有这些。** C_c 只是一维线性映射，不包含"两个参考系之间的变换"。

| 候选 | 是否成立 | 是否够深 |
|------|---------|---------|
| c = x/t 恒定 | ✅ | ⚠️ 弱 —— 是映射参数，不是状态不变量 |
| 半群结构被保留 | ✅ | ⚠️ 结构性质，不是新不变量 |
| TIME 不变量不被破坏 | ✅ | ⚠️ 它们不依赖空间 |
| Minkowski 间隔 | ❓ | 需要 3+1 维时空，当前只有 1+1 |

---

## 4. 三问结果

```
FUSE(SPACE):

  ✓ 变   C_c: t → x = ct            — 映射存在
  ✓ 合   C_c(τ₁+τ₂) = C_c(τ₁) + C_c(τ₂)  — 半群同态
  ? 守   无新的空间不变量            — OPEN
```

**第三问标 OPEN。**

不是找不到不变量 —— 是当前结构（一维，无参考系变换，无度规）不足以产生真正的空间不变量。

这**不是**融合失败。这是精确地知道：**C_c 作为一个结构保持映射成立，但要产生空间特有 invariant，需要增加维度或引入参考系变换。**

---

## 5. 当前的三者关系

```
        INVARIANT（来自 TIME）
        ├── |archive| 非递减
        └── Identity 连续性
              │
              │ （两者在 C_c 下不被破坏，但也不依赖 C_c）
              │
    ┌─────────┴─────────┐
    │                   │
  TIME                SPACE
    │                   │
  T_τ                 C_c = c·τ
  (ℕ, +)             (cℕ, +)
    │                   │
    └────── SCALE ──────┘
           R_λ
         (ℝ_{>0}, ×)
```

TIME 和 SPACE 共享加法结构（通过 C_c 保持），SCALE 以乘法作用于两者。

**但目前的不变量全部来自 TIME 侧。SPACE 侧没有产生新的不变量。**

---

## 6. 时间-尺度-空间一致性

将 SCALE 的假设 H 扩展到 SPACE：

```
SCALE 假设:  R_λ T_τ = T_{λτ} R_λ        （在 Outbox 上成立）

SPACE 版本:  C_c R_λ T_τ = C_c T_{λτ} R_λ = R_λ C_c T_τ

即：         C_c R_λ = R_λ C_c              （如果 R_λ 在时间和空间上一致作用）
```

**这意味着**：先缩放时间再映射到空间 = 先映射到空间再缩放空间。c 和 λ 可交换。

| 条件 | 成立？ |
|------|--------|
| C_c R_λ = R_λ C_c | ✅ 若 R_λ 在时间和空间上都做乘法缩放 |
| 前提 | ⚠️ R_λ 在空间上的作用尚未定义 |

**分类**：⚠️ 形式化成立，但 R_λ 在空间上的行为是假设的。

---

## 7. 判断

```
问：C_c 能否作为结构保持映射？
答：能。C_c(τ₁+τ₂) = C_c(τ₁) + C_c(τ₂)。半群同态。

问：有空间特有的不变量吗？
答：没有。c 是参数不是不变量。TIME 的不变量在 SPACE 下不被破坏，
    但 SPACE 没有产生属于自己的新不变量。

问：这是融合失败吗？
答：不是。一维线性映射的结构不足以产生空间不变量。
    需要更多维度或参考系变换。OPEN 精确标出了这个边界。
```

---

## 8. 给下一融合的出口

```
SPACE
  │
  ├── C_c: Time → Space          → 半群同态 ✓
  ├── C_c R_λ = R_λ C_c          → 形式化成立
  └── 空间不变量                 → OPEN
       │
       └── 需要: 多维空间 + 参考系变换 → MATTER
```

**SPACE 的交付不是一组公式，是一个诚实的位置：**

**C_c 让 TIME 的结构完整映射到 SPACE。但 SPACE 目前还不产生自己的不变量。** 要让空间产生不变量，需要至少两个空间维度，以及不同参考系之间的变换规则。

那一步属于 MATTER —— 当空间中的对象具有属性（质量、动量），并且这些属性在变换下必须保持一致时，不变量才会出现。

---

## 9. 回顾：融合算法三问

```
FUSE(TIME):   变 ✓  合 ✓  守 ✓  → 冻结
FUSE(SCALE):  变 ✓  合 ✓  守 ✓  → 冻结（守来自 TIME 的尺度不变性）
FUSE(SPACE):  变 ✓  合 ✓  守 ?  → 冻结（OPEN）
```

三个融合都不超过 3 个问题。SPACE 的 OPEN 不是缺口 —— 是地图上精确标记的"此处尚未探索"。

---

## 冻结

SPACE.md 不证明光年。不引入相对论。不发明不变量。

```
073ed52 → TIME → SCALE → SPACE → MATTER → CHEMISTRY → LIFE
  ✓        ✓      ✓      ✓(OPEN)    ?          ?        ?
```

第三问 OPEN。冻结。

下一步：MATTER —— 引入多维和守恒量，问空间不变量能否从物质属性中产生。
