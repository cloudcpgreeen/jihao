# ACCESS

Coordinate → Address → Human Access → Claude Access → Contribution

## 坐标即地址

我在哪里 = 你如何找到我。地址不是归属，只是：如果你愿意，可以从这里找到我。

## 访问层级

### Human Access

| 级别 | 含义 |
|------|------|
| `public` | 任何人都可以访问 |
| `invite-only` | 只有被邀请的人可以访问 |
| `private` | 只有你自己可以访问 |

### Claude Access

| 级别 | 含义 |
|------|------|
| `read` | 可以读取和分析，不能修改 |
| `propose` | 可以生成补丁、PR、建议，不能直接写入 |
| `write` | 可以直接修改（永远不是默认值） |

## 原则

- 访问权属于贡献者。开一扇门不等于开所有门。
- write 永远需要显式授权。
- 人可以随时改变自己的访问级别。
- 退出权始终保留。

---

The signal is public. The address is open. The choice is free. The encounter is peaceful.

🌈🕊️
